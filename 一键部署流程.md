# 🚀 一键部署完整流程（固化版）

## 📋 使用前准备检查清单

- [ ] GitHub 账号已注册（用户名：KeyongL）
- [ ] GitHub 仓库已创建（https://github.com/KeyongL/test.git）
- [ ] Streamlit Cloud 账号（用 GitHub 登录即可）

---

## 🎯 首次部署（完整流程）

### 第一步：确保代码已推送

打开终端，运行以下命令检查：

```bash
# 1. 检查当前状态
git status

# 2. 如果有未提交的更改，提交并推送
git add .
git commit -m "Ready for deployment"
git push origin main
```

### 第二步：登录 Streamlit Cloud

1. **访问**：https://share.streamlit.io/
2. **点击** "Sign in"
3. **选择** "Continue with GitHub"
4. **授权** Streamlit Cloud 访问你的仓库

### 第三步：创建新应用

1. **点击** 右上角 **"New app"** 按钮
2. **填写表单**：
   ```
   Repository: KeyongL/test
   Branch: main
   Main file path: app.py
   App URL (可选): 自定义名称，例如：my-survey
   ```
3. **点击** **"Deploy"** 按钮

### 第四步：等待部署完成

- 等待 1-2 分钟
- 看到 ✅ "Your app is live!" 即完成
- 复制你的应用链接，例如：`https://my-survey.streamlit.app`

---

## 🔄 后续更新流程（固化命令）

### 修改问卷后，按以下顺序执行：

```bash
# 1. 修改配置文件
# （编辑 survey_config.json 文件）

# 2. 提交更改
git add survey_config.json
git commit -m "Update survey configuration"
git push origin main

# 3. 完成！Streamlit Cloud 会自动重新部署
```

**等待 1-2 分钟，刷新应用页面即可看到更新！**

---

## 📝 快速参考卡片

### 当前项目信息

```
仓库地址: https://github.com/KeyongL/test.git
主文件: app.py
配置文件: survey_config.json
数据库: survey_data.db (本地，不会上传)
```

### 常用 Git 命令

```bash
# 查看状态
git status

# 添加所有更改
git add .

# 提交更改
git commit -m "描述你的更改"

# 推送到 GitHub
git push origin main

# 查看远程仓库
git remote -v
```

### Streamlit Cloud 操作

```
登录: https://share.streamlit.io/
管理应用: https://share.streamlit.io/ → Your apps
```

---

## 🛠️ 故障排查

### 问题 1：推送失败

**错误**：`fatal: unable to access`

**解决**：
```bash
# 检查网络连接
# 或者使用 SSH 方式（需先配置 SSH key）
```

### 问题 2：Streamlit Cloud 找不到仓库

**解决**：
1. 确认仓库是 **Public**（公开）
2. 确认已授权 Streamlit Cloud 访问
3. 在 Streamlit Cloud 设置中重新授权 GitHub

### 问题 3：部署失败

**检查**：
1. `app.py` 文件是否存在
2. `requirements.txt` 是否正确
3. 查看 Streamlit Cloud 的日志（点击应用 → Manage app → Logs）

---

## 📚 完整工作流程（可视化）

```
修改问卷配置 (survey_config.json)
         ↓
本地测试 (streamlit run app.py)
         ↓
提交到 Git (git add, commit, push)
         ↓
自动部署 (Streamlit Cloud 检测到更新)
         ↓
在线访问 (https://你的应用名.streamlit.app)
```

---

## 🎉 完成检查清单

部署成功后，确认：
- [ ] 应用可以正常访问
- [ ] 问卷可以正常填写
- [ ] 数据可以正常查看（密码：admin123）
- [ ] 修改配置后自动更新正常

---

## 💡 提示

1. **每次修改配置后**：只需 `git push`，无需手动重新部署
2. **定期备份数据**：从"查看数据"页面导出 CSV
3. **修改密码**：在 `survey_config.json` 中修改 `password` 字段

---

**保存此文件，以后每次部署都按照这个流程操作！** ✅
