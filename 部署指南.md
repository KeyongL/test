# 🚀 Streamlit Cloud 一键部署指南

## 前置条件

✅ **代码已推送到 GitHub**  
✅ **GitHub 账号已注册**  
✅ **Streamlit Cloud 账号已注册**（用 GitHub 账号登录即可）

---

## 步骤 1：创建 GitHub 仓库（如果还没有）

1. 访问：https://github.com/new
2. 仓库名称：`survey-app`（或任意名称）
3. 选择：**Public**（公开，便于免费部署）
4. 点击 "Create repository"

---

## 步骤 2：推送代码到 GitHub

如果你还没有推送，运行：

```bash
git remote add origin https://github.com/你的用户名/survey-app.git
git push -u origin main
```

或如果已有远程地址但需要更新：
```bash
git remote set-url origin https://github.com/你的用户名/survey-app.git
git push -u origin main
```

---

## 步骤 3：在 Streamlit Cloud 部署（3步完成）

### 3.1 访问 Streamlit Cloud
- 打开：https://share.streamlit.io/
- 用 GitHub 账号登录

### 3.2 新建应用
1. 点击右上角 **"New app"**
2. 选择仓库：选择你的 `survey-app` 仓库
3. 选择分支：`main`
4. Main file path：输入 `app.py`

### 3.3 部署
- 点击 **"Deploy"**
- 等待 1-2 分钟自动部署完成

---

## 完成！

部署完成后，你会得到一个公开链接，例如：
```
https://你的应用名.streamlit.app
```

你可以：
- ✅ 分享链接给任何人填写问卷
- ✅ 在后台查看数据
- ✅ 修改 `survey_config.json` 后重新部署即可更新

---

## 后续更新

以后修改配置后：
```bash
git add survey_config.json
git commit -m "Update survey questions"
git push
```

Streamlit Cloud 会自动重新部署！

---

## 注意事项

⚠️ **数据库文件**：
- Streamlit Cloud 的数据库是临时存储
- 如果需要持久化数据，建议定期导出 CSV
- 或后续可升级到 PostgreSQL

⚠️ **配置文件**：
- 修改 `survey_config.json` 后记得 push 到 GitHub
- 部署会自动更新
